<div class="container offset-md-2">
<h2>Ingresar</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), data: { turbo: :false }) do |f| %>
  <div class="form-inputs w-50">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" },
                label: "Correo Electronico" %>
    <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" },
                label: "Contraseña" %>
    <%= f.input :photos, label: "Fotos", as: :file, input_html: { multiple: true } %>
    <%= f.input :remember_me, label: "Guardar", as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Ingresar", class: "btn-primary" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>
