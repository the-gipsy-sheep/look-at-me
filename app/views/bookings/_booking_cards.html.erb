<div class="container">
  <div class="row">
      <% @bookings.each do |booking| %>
        <div class="col-4 col-sm-4 col-lg-4">
          <div class="card m-3">
            <%# <% product.photos.each do |photo| %>
              <%# <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
            <%# <% end %>
            <div class="card-body">
              <h4 class="card-title">Reserva hecha por <%= booking.user.username %></h5>
              <h5 class="card-title"><%= booking.product.title %></h5>
              <p class="card-text"><%= booking.product.description %></p>
              <p class="card-text"><%= booking.product.price %></p>
              <div>
                <p>Desde: <%= booking.start_date %></p>
                <p>Hasta: <%= booking.finish_date %></p>
                <p>Cantidad de Días: <%= booking.finish_date - booking.start_date %></p>
                <%# <%= link_to "editar", edit_product_booking_path(booking.product_id, booking), class:"btn btn-primary" %>
                <%= link_to '<i class="fa fa-trash"></i>'.html_safe, product_booking_path(booking.product_id, booking), class:"btn btn-primary", data: {turbo_method: :delete, turbo_confirm: "Seguro?"} %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
  </div>
</div>
